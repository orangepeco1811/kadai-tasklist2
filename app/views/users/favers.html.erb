<div class="row">
  <aside class="col-xs-4">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title"><%= @user.name %></h3>
      </div>
      <div class="panel-body">
        <img class="media-object img-rounded img-responsive" src="<%= gravatar_url(@user, { size: 500 }) %>" alt="">
      </div>
    </div>
         <%= render 'favorites/follow_button', user: @user %>
  </aside>
  <div class="col-xs-8">
    <ul class="nav nav-tabs nav-justified">
      <li class="<%= 'active' if current_page?(user_path(@user)) %>"><%= link_to user_path(@user) do %>Microposts <span class="badge"><%= @count_microposts %></span><% end %></li>
      <li class="<%= 'active' if current_page?(favings_user_path(@user)) %>"><%= link_to favings_user_path(@user) do %>Followings <span class="badge"><%= @count_favings %></span><% end %></li>
      <li class="<%= 'active' if current_page?(favers_user_path(@user)) %>"><%= link_to favers_user_path(@user) do %>Followers <span class="badge"><%= @count_favers %></span><% end %></li>
    </ul>
    <%= render 'users', users: @favers %>
  </div>
</div>